http://www.livejournal.com/doc/server/ljp.csp.flat.syncitems.html

http://www.livejournal.com/doc/server/ljp.csp.flat.getevents.html
    selecttype=syncitems
    lastsync=

@class LJAccount 
{
	NSDate *_lastSyncDate;
}

- (void)syncItems
{
	parameters = [NSDictionary dictionaryWithObject:[_lastSyncDate]
forKey:@"lastsync"];
	[self getReplyForMode:@"syncitems" parameters:
	total
	count
	[].item = L-<itemid>
	[].action = "create" | "update"
	[].time = "YYYY-MM-DD HH:MM:SS"

	if (total == count) no need to continue

@end

@class LJItem
- (char)type;
- (unsigned int)itemID;
- (int)action; LJCreateItemAction | LJUpdateItemAction
- (NSDate *)changeTime;
- (LJEntry *)getEntry;
@end